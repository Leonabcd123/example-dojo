id: linux-luminarium
name: '"><img src=x onerror=alert(''Is pwn.college vulnerable to stored XSS?'')><span title="'
image: pwncollege/challenge-legacy:latest

description: |
  <a>Click me</a>

type: welcome
award:
  emoji: 🐧

modules:
 - id: "builtins"
  
