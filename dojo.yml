id: linux-luminarium
name: ../../
image: pwncollege/challenge-legacy:latest

description: |
  <a>Click me</a>

type: welcome
award:
  emoji: 🐧
  
modules:
 - id: "builtins"

survey-sources: surveys_dir

survey:
  src: /<img src=x onerror=alert(1)>
  data: asdf
  prompt: asdf
