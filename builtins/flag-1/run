#!/opt/pwn.college/bash

flag=$(cat /flag)

for (( i=0; i<${#flag}; i++ )); do
  for (( i=0; i<$(($RANDOM % 2)); i++ )); do
    echo -n "garbage data" >&2
  done
  echo "${flag:$i:1}"
  for (( i=0; i<$(($RANDOM % 2)); i++ )); do
    echo -n "garbage data" >&2
  done
done
